import { Meta, Title } from '@storybook/addon-docs';
import { elevations } from './design-tokens.json';
import './fonts.css'; import { card, wrapper } from "./radius.stories.mdx";


<Meta title="Design System/Elevations" />

<Title>Elevations</Title>

<div style={{ ...wrapper, alignItems: "start" }}>
  {Object.entries(elevations).map((item) => <div style={{
    ...card,
    border: 0,
    borderRadius: "4px",
    boxShadow: item[1]
  }}
  >
    {item[0]}
  </div>)}
</div>
