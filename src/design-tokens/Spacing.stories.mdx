import { Meta, Title } from '@storybook/addon-docs';
import { spacingPoint } from './design-tokens.json';
import './fonts.css'; import { wrapper } from "./radius.stories.mdx";


<Meta title="Design System/Spacing" />

<Title>Spacing X={spacingPoint.toString()}</Title>

export let card = {
  display: "flex",
  alignItems: "center",
  justifyContent: "center",
  marginTop: "20px",
  borderRadius: "4px",
  backgroundColor: "rgba(23,181,149,0.2)",
  color: "#17B595",
  fontWeight: 700,
  fontSize: "12px",
  flexShrink: 0
};

<div style={{ ...wrapper, alignItems: "start" }}>
  {Array.apply(null, Array(18)).map((item, index) => <div style={{
    ...card,
    padding: `${(index + 1) * spacingPoint}px`
  }}
  >
    {(index + 1) * spacingPoint}px
  </div>)}
</div>
