import { Meta, Typeset } from '@storybook/addon-docs';
import { radius } from './design-tokens.json';
import './fonts.css';


<Meta title="Design System/Radius" />

# Radii

export let wrapper = {
  display: "flex",
  gap: "30px",
  marginTop: "30px"
};

export let card = {
  display: "flex",
  alignItems: "center",
  justifyContent: "center",
  border: "1px solid #C0C8CC",
  width: "120px",
  height: "120px",
  marginTop: "20px",
  flexShrink: 0
};

<div style={wrapper}>
  {Object.entries(radius).map(item => <div key={item[0]} style={{ ...card, borderRadius: Number(item[0]) }}>
    {item[1].value}
  </div>)}
</div>
